apiVersion: v1
kind: Service
metadata:
  name: securedrop-api-service
  labels:
    app: securedrop-api
spec:
  type: ClusterIP
  selector:
    app: securedrop-api
  ports:
    - port: 3001
      targetPort: 3001
